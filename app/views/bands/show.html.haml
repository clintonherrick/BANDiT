%h1= @band.title
%p= @band.content

= link_to "Back", root_path
= link_to "Edit", edit_band_path(@band)
= link_to "Delete", band_path(@band, @genre), data: {confirm: " Are you sure? "}, method: "delete"
