<h1>
  <%= @band.title %>
</h1>
<p>
  <%= @band.content %>
</p>
<div class="btn-group">
  <%= link_to "Back", root_path, class: "btn btn-default" %>
  <%= link_to "Edit", edit_band_path(@band), class: "btn btn-default" %>
  <%= link_to "Delete", band_path(@band, @genre), data: {confirm: " Are you sure? "}, method: "delete", class: "btn btn-default" %>
</div>
